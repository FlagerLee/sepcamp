server:
  port: 8080

#https://blog.csdn.net/iku5200/article/details/82856621/
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/sepcamp?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath*:mappers/*Mapper.xml
  type-aliases-package: com.seclass.sepcamp.models
  map-underscore-to-camel-case: true

logging:
  level:
    com:
      example:
        mapper : debug



#作业提交功能： 发布本周作业（设置开始结束时间）-- 增，学生提交作业 -- 改，查，
#实验项目管理： 各阶段增加，删除，修改（提交），查询   针对新项目申请、开题、中期检查（可选）、结题这些提交阶段，提供一个统一规范的提交管理流程以及工作流记录。